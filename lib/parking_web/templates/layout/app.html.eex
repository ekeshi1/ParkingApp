<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Parking · Phoenix Framework</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </head>
  <body>
    <header>
      <nav role="navigation">
        <ol class="breadcrumb pull-right">
          <%= if Parking.Authentication.load_current_user(@conn) do %>
            <li>Hello <%= Parking.Authentication.load_current_user(@conn).name %></li>
            <li><%= link "Log out", to: Routes.session_path(@conn, :delete, Parking.Authentication.load_current_user(@conn)), method: "delete" %></li>
          <% else %>
           <!--
            <li><%= link "Log in", to: Routes.session_path(@conn, :new) %></li>
          -->
          <% end %>
        </ol>
        <span class="logo"></span>
      </nav>
    </header>
    <main role="main" class="container">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= @inner_content %>
    </main>
  </body>
</html>
